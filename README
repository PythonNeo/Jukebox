
Get the pyspotify module.
run jukebox.py

1. wget -q -O - https://apt.mopidy.com/mopidy.gpg | sudo apt-key add -

2. sudo wget -q -O /etc/apt/sources.list.d/mopidy.list https://apt.mopidy.com/wheezy.list
or
2. sudo wget -q -O /etc/apt/sources.list.d/mopidy.list https://apt.mopidy.com/jessie.list

3. sudo apt-get update
4. sudo apt-get install python-spotify

5. sudo apt-get install python-pip python-alsaaudio

6. sudo pip install Flask
7. sudo pip install flask-socketio
